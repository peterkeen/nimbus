apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cert-manager
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: cert-manager-credentials
  dataFrom:
    - extract:
        key: umbys4jfnh6k27u2isqdimhz6i
